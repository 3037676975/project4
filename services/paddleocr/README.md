# PaddleOCR local service

Internal-only OCR service for Project4 private deployments. The API is protected by the same `PARSER_API_KEY` used by the document parser. Images and scanned PDFs are processed locally with PP-OCRv6 on CPU.
